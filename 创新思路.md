


O2里面的DEIM、DFINE版本就是基于DEIM改的，EdgeCrafter也是基于DEIM改的。只不过他没把O2-DEIM/DFINE的代码放出来。他的论文
  https://arxiv.org/abs/2603.15497 很有意思，就是他的创新方式不是我只简单加个模块，而是一种机制上的创新。这也是咱们努力的方向


• 对，这个判断很关键。我们的目标不能是“EdgeCrafter + O² + 一个高分辨率模块”，而应该像 O² 一样：先发现基础范式在新任务中的系统性失效，再改写它的学
  习机制。

  ### O² 真正值得学的地方

  O² 不是简单并列三个模块，而是一个完整因果链：

   基础 DETR 的旋转失效                        O² 的机制修改
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   标量角度不能表达旋转不确定性                Angle Distribution Refinement
  ──────────────────────────────────────────  ─────────────────────────────────────────────────────
   普通 L1/KL 匹配存在几何歧义                 顶点集合 Chamfer matching
  ──────────────────────────────────────────  ─────────────────────────────────────────────────────
   歧义匹配导致 decoder 层间 query 身份跳变    Oriented Contrastive Denoising + instability metric

  即“表示不合理 → 匹配不合理 → 训练不稳定”，三个方法都服务于同一个问题定义。论文甚至先定义了 query instability 指标，再用它验证机制，而不是只展示
  AP 上涨。O²论文 (https://arxiv.org/abs/2603.15497)

  它在不同的 RT-DETR、D-FINE、DEIM 底座上都能成立，恰恰证明贡献是检测机制，而不是 backbone。公开仓库的主体确实还是 rotated_rtdetr，完整
  O²-DEIM/O²-D-FINE 并没有以论文 family 同等完整的形态公开。AI4RS代码 (https://github.com/wokaikaixinxin/ai4rs/tree/main/projects/rotated_rtdetr)

  ### 我认为我们应该抓的根问题

  目前最值得深挖的不是“哪里少算一点”，而是：

  > 局部 DETR 的 one-to-one set prediction，在高分辨率切片后不再等价于原图上的 one-to-one set prediction。

  O² 在一个 1024 patch 内是端到端、NMS-free 的；但 CODrone 的 4K 图像被切成大量重叠 patch 后：

  - 同一个原图目标在多个 tile 中分别参与 Hungarian matching；
  - 每个 tile 都形成一套局部 query 身份；
  - 映射回原图后出现重复、截断和角度不一致；
  - 最后仍需要 rotated NMS/WBF 合并；
  - 改变切片起点，同一个目标的检测结果也可能明显改变。

  所以，“局部 NMS-free”并没有带来“原图级 NMS-free”。这不是一个小模块问题，而是 set prediction 的基本单位错了：当前单位是 tile，真正的单位应该是原
  图中的 object。

  ### 新主线：Partition-Invariant Oriented Set Prediction

  一句话定义可以是：

  > 将高分辨率 tiles 视为同一图像的计算分片，而不是互相独立的检测图像，使最终 OBB 集合对切片位置近似不变。

  形式上，希望不同切片方案 (\Pi_a,\Pi_b) 满足：

  [
  \mathcal S_\theta(I;\Pi_a)\simeq
  \mathcal S_\theta(I;\Pi_b),
  ]

  其中结果都映射到原图坐标系。暂时可以叫：

  - PIOS：Partition-Invariant Oriented Set Prediction；
  - 或 GOTS：Global Oriented Tiled Set Prediction。

  名字以后再定，核心是“partition invariance”。

  它可以像 O² 一样形成一条统一机制链：

  1. 原图级 query 表示

     所有 tile 产生的候选都映射到统一的原图坐标；query 从产生开始就是 global object query，而不是 tile-local query。

  2. OBB-support-aware 跨块细化

     一个 query 的预测旋转区域如果与多个 tile 相交，就从这些 tile 联合采样证据，而不是只路由给中心所在 tile。这样人工切片边界不会截断它的几何证
     据。

  3. 原图级一次 Hungarian matching

     同一原图的所有 global queries 与原图 GT 只做一次匹配，直接保证一个目标对应一个最终 query，而不是每个 tile 各匹配一次再 NMS。

  4. Partition contrastive denoising

     训练时随机移动切片网格，使边界从目标上穿过，构造“完整视图—截断视图”“不同切片相位”的正负 query，要求它们恢复到同一个全局 OBB。它不是额外拼接
     一个 consistency loss，而是专门解决 partition-induced query instability。

  这四点应被写成一个机制的四个环节，而不是“四个创新模块”。

  ### 我们也需要一个像 O² instability 那样的诊断指标

  可以定义 Tiling-Phase Instability（TPI）。

  对同一张图采用若干不同切片起点，将匹配到同一 GT 的预测恢复到原图坐标，统计：

  - 漏检状态是否变化；
  - 类别和置信度变化；
  - 预测 OBB 之间的 rotated IoU；
  - 中心、角度和顶点方差；
  - decoder 层间以及不同 partition 间的 query 身份变化。

  然后按以下因素分组：

  - GT 到 tile 边界的距离；
  - <8 / 8–16 / 16–32 / >32 像素；
  - 长宽比；
  - 30/60/100 m 高度；
  - 30°/90°视角。

  如果 TPI 随着目标靠近切片边缘、尺寸减小而显著升高，我们就找到了真正的论文动机；如果没有明显现象，就立即放弃这条假设，不强行编故事。

  ### EdgeCrafter 在这里是什么角色

  EdgeCrafter是非常合适的工程底座，但不是论文创新本身：

  - ECDet 已经具有 DEIM/D-FINE 血缘；
  - 配置中有 Hungarian matcher、MAL、FGL、denoising 和 distribution regression；
  - ECViT 提供紧凑 backbone；
  - 我们只需要将其从 HBB global query 扩展到 OBB，并重构成原图级 tiled set prediction。EdgeCrafter配置
    (https://github.com/Intellindust-AI-Lab/EdgeCrafter/blob/main/ecdetseg/configs/ecdet/ecdet.yml)

  O² 的 Chamfer matching、oriented denoising可以作为旋转基线机制；ADR主要对应 D-FINE 版本。我们的贡献不能写成“把 O² 移植到 EdgeCrafter”，而是：

  > 在高分辨率切片条件下，重新定义 OBB query 的身份、证据范围和匹配域。

  ### 与已有工作必须拉开的界线

  - ESOD：解决空背景上的冗余计算，但仍以局部 feature slice 为检测单位。ESOD (https://arxiv.org/abs/2407.16424)
  - UHR-DETR：已有 maximum-coverage router、全局坐标和 global-local decoder，因此“区域选择 + 全局上下文”不能再作为我们的新意。
  - O²：解决 patch 内旋转表示和匹配，但没有解决原图被切片后全局 one-to-one 性质丢失。
  - 普通 shift-equivariant backbone：只改善特征平移稳定性，没有处理跨 tile 的 OBB query 身份和原图级匹配。

  因此，只有“跨 partition 的全局 OBB set prediction”这个交叉点有机会称得上机制创新。

  ### 下一步应该怎么做

  先不急着写任何新模块，第一阶段只做三个实验：

  1. EdgeCrafter-DEIM HBB 基线跑通；
  2. 得到 EdgeCrafter-O² 的局部 OBB 基线；
  3. 在 CODrone 上用多种 tile offset 测 TPI、重复率、边界 AP75 和 rotated-NMS 依赖。

  如果诊断成立，再实现“global query + 原图级 matching”；最后才加入 support-aware 跨 tile refinement 和 partition denoising。

  我现在对这条线的评价是：比之前的预算路由方案高一个层次，也更符合你说的“O²式创新”。真正的论文标题不应该是“An Efficient Module for…”，而应该接
  近：

  > Rethinking Tiled Oriented Detection as Partition-Invariant Global Set Prediction



# 相比于EdgeCrafter，RTDETRv4 https://github.com/RT-DETRs/RT-DETRv4 作为项目基础是不是更好


• 是。对我们当前的研究目标——“4K 无人机小目标 OBB + 高效切片/全局集合预测机制”——我更建议：

  > 用 RT-DETRv4 仓库作为工程底座，但第一版模型从 D-FINE-S/DEIM-S 开始，而不是直接把完整 RT-DETRv4 当基线。

  ### 为什么 RT-DETRv4 更适合做底座

   维度                          RT-DETRv4                                          EdgeCrafter
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   与 O² 的代码血缘              仓库原生统一支持 RT-DETRv2、D-FINE、DEIM           检测头也来自 D-FINE/DEIM，但叠加了 ECViT
  ────────────────────────────  ─────────────────────────────────────────────────  ───────────────────────────────────────────────────────────
   特征结构                      CNN 原生输出 S3/S4/S5，再由 Hybrid Encoder 融合    ViT 主要输出 stride-16 单尺度特征，再插值生成 stride-8/32
  ────────────────────────────  ─────────────────────────────────────────────────  ───────────────────────────────────────────────────────────
   改 matcher/query/denoising    更直接、变量更少                                   会和 ECViT、蒸馏预训练耦合
  ────────────────────────────  ─────────────────────────────────────────────────  ───────────────────────────────────────────────────────────
   高分辨率扩展                  层级 CNN 更适合切片、局部计算和共享低层特征        全局 ViT token 对分辨率增长更敏感
  ────────────────────────────  ─────────────────────────────────────────────────  ───────────────────────────────────────────────────────────
   COCO-S                        49.8 AP，3.66 ms                                   51.7 AP，5.41 ms
  ────────────────────────────  ─────────────────────────────────────────────────  ───────────────────────────────────────────────────────────
   适合作为论文主干              更干净，便于证明机制贡献                           Backbone 本身就是一个强创新变量

  RT-DETRv4 仓库明确支持直接复现 D-FINE、DEIM 和 RT-DETRv2，并提供自定义数据、ONNX、TensorRT
  和延迟测试工具，这一点非常适合我们构造统一实验平台。RT-DETRv4 官方仓库 (https://github.com/RT-DETRs/RT-DETRv4)

  EdgeCrafter 的 COCO 精度确实更高：ECDet-S 是 51.7 AP，而 RT-DETRv4-S 是 49.8 AP；但前者延迟也是 5.41 ms 对 3.66 ms。更重要的是，EdgeCrafter
  的优势来自一整套“DINOv3 检测化教师 → ImageNet+COCO 上预蒸馏 → ECViT 学生 → 下游训练”的机制，不只是换了一个检测头。EdgeCrafter 官方结果
  (https://github.com/Intellindust-AI-Lab/EdgeCrafter)；EdgeCrafter 方法与预训练细节 (https://arxiv.org/html/2603.18739)

  ### 一个关键判断：RT-DETRv4 并不是全新的推理架构

  RT-DETRv4 的主体仍然来自 D-FINE/DEIM。它的新东西主要是训练期：

  - DSI：用冻结的 DINOv3-B 对齐 AIFI 输出的深层 (F_5)；
  - GAM：按照梯度比例动态调整蒸馏强度；
  - 推理时移除教师和蒸馏分支，因此延迟不增加。

  论文的统一消融中，DSI+GAM 在 DEIM-L 上带来约 +0.5 AP。RT-DETRv4 方法与消融 (https://arxiv.org/html/2510.25257)

  这意味着：

  > RT-DETRv4 很适合当“代码容器”，但它的核心创新解决的是深层语义瓶颈，不是高分辨率小目标、旋转定位或跨切片重复预测。

  CODrone 真正的困难主要在 stride-8 小目标细节、方向几何、切片边界和原图级一对一预测。仅仅对齐 stride-32 的 (F_5)，未必能明显改善角度误差和边界目
  标。

  ### 我建议的具体版本路线

  1. 代码仓库：RT-DETRv4。
  2. 第一基线：D-FINE-S + HGNetv2。

     D-FINE 的分布式边界回归接口更方便接入 O² 风格的角度分布、旋转框精修和质量建模。先把纯 OBB 基线做干净。

  3. 第二强基线：DEIM-S。

     在相同 OBB 实现上加入 DEIM 的匹配质量分类/训练策略，排除“提升只是来自更强训练配方”。

  4. 主方法：加入我们的原图级、分区不变集合预测机制：
      - 全图 GT 到多切片候选的统一匹配；
      - 跨切片 query 身份与去重；
      - 边界截断感知的 OBB query 更新；
      - partition-consistency denoising；
      - 尽可能保持 NMS-free，而不是最后再做旋转 NMS 拼接。

  5. 最终强模型：在主方法上打开 RT-DETRv4 的 DSI/GAM。

     这作为正交增强项，而不是核心贡献。需要明确写成“ours + VFM semantic distillation”。

  6. 泛化实验：再把机制移植到 EdgeCrafter/ECViT。

     如果在 HGNetv2 和 ECViT 上都有效，审稿人更难认为它只是依赖某个 backbone。这时候 EdgeCrafter很有价值，但更适合做第二底座，而不是第一开发底
     座。

  ### 什么情况下反而应选 EdgeCrafter

  如果我们决定把论文主线改成：

  > “面向无人机边缘设备的紧凑 ViT，通过遥感任务专用教师蒸馏获得高效 OBB 表征”

  那么 EdgeCrafter 更合适，因为其核心就是“任务专用教师—紧凑学生—跨任务迁移”。但这会把工作重心带向遥感教师适配、无标注航空影像蒸馏和 ECViT 设计，
  和我们目前讨论的“高分辨率分区破坏 DETR 集合预测”是另一条论文路线。

  最终建议很明确：

  > 主仓库选 RT-DETRv4，首个可复现 OBB 基线选 D-FINE-S，随后补 DEIM-S；RT-DETRv4 蒸馏作为最终增强，EdgeCrafter作为跨骨干验证。

  这样既利用最新框架，又不会让别人的蒸馏机制掩盖我们自己的机制创新。这个结论是按“官方论文事实、仓库能力、针对 CODrone 的工程推断”分层得出的。

─ Worked for 4m 21s ─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────


# 能够把你的创新思路说清楚，我们是要做一个端到端的高分辨率旋转框检测器吗，虽然再推理时需要切片但是无需NMS合并，直接出结果吗


• 对，你理解得基本准确。更严格地说，我们要做的是：

  > 切片只是计算单元，原始高分辨率图像才是预测与匹配单元。

  推理时仍然切片，但各切片不独立输出最终检测框，也不存在“把切片框拿出来做旋转 NMS/WBF”。网络在全图层面只进行一次集合预测，直接输出原图坐标系下的
  旋转框集合。

  ## 整体流程

  3840×2160 原图
        ↓
  内部生成重叠切片/带 halo 的计算块
        ↓
  共享 Backbone + Encoder 提取各切片多尺度特征
        ↓
  切片特征映射回统一的全图坐标系
        ↓
  构建全图多尺度特征记忆
        ↓
  全图级 Query 初始化
        ↓
  全图 OBB Decoder
        ↓
  {类别, cx, cy, w, h, θ} 的全图目标集合
        ↓
  置信度过滤
  无需旋转 NMS / WBF / 切片框合并

  最重要的一点是：

  - 普通 SAHI：每个切片都是一个独立检测问题；
  - 我们的方法：每个切片只是原图的一个特征视图；
  - 普通方法最后合并框；
  - 我们在特征和 Query 层面融合，最终框从来就是全图框。

  ## 训练时怎么做

  设原图标注集合为：

  [
  Y={(c_i,x_i,y_i,w_i,h_i,\theta_i)}_{i=1}^{N}
  ]

  无论一个目标出现在几个重叠切片中，它在 (Y) 中都只有一个实例。

  所有切片经过网络后，统一产生 (Q) 个全图 Query：

  [
  \hat{Y}={(\hat c_j,\hat x_j,\hat y_j,\hat w_j,\hat h_j,\hat\theta_j)}_{j=1}^{Q}
  ]

  然后只做一次原图级 Hungarian Matching：

  [
  \sigma^*=\arg\min_{\sigma}\sum_i
  \mathcal C\left(Y_i,\hat Y_{\sigma(i)}\right)
  ]

  因此一个 GT 只能匹配一个 Query。其他看到了同一目标的候选 Query 会被监督为背景，而不是依靠推理后的 NMS 消除。

  这才是重复抑制机制的根本变化：

  > 重复框不是预测完之后再删除，而是在集合预测过程中就不应产生。

  ## 跨切片目标怎么处理

  例如一辆倾斜车辆恰好跨越两个切片边界。

  传统流程会得到：

  - 切片 A：一个残缺框；
  - 切片 B：另一个残缺框；
  - 最后旋转 NMS 不一定能合并，因为两个框 IoU 可能很低。

  我们的方法中，一个全局 Query 的参考框始终位于原图坐标系。它可以从所有覆盖该目标的切片特征中采样：

  [
  p_{qk}=c_q+
  R(\theta_q)
  \begin{bmatrix}
  w_q & 0\
  0 & h_q
  \end{bmatrix}
  \Delta p_{qk}
  ]

  其中采样偏移随旋转框方向旋转。采样点落在哪个切片，就从哪个切片的特征中取值。因此一个 Query 可以同时利用边界两侧的信息，最后回归一个完整旋转框。

  这部分可以称为：

  Global-coordinate Oriented Cross-tile Attention

  但它不是孤立加的 attention 模块，而是“全图 Query 拥有目标、切片只提供证据”这一原则的自然实现。

  ## 核心创新到底是什么

  不是三个互不相关的模块，而是一个统一机制：

  ### Partition-Invariant Oriented Set Prediction

  给定任意切片方案 (\pi)：

  [
  \hat Y_\pi=F(T_\pi(I))
  ]

  我们要求不同切片位置、重叠率和网格偏移下：

  [
  \hat Y_{\pi_1}\approx \hat Y_{\pi_2}
  ]

  也就是说，计算怎么分块可以变化，但最终目标集合不应变化。

  这个机制需要三个互相依赖的实现部分：

  1. 全局坐标特征记忆

     切片特征映射回统一坐标系，重叠区域在特征层融合，而不是框层 NMS。

  2. 全局 Query 所有权

     Query 属于原图中的目标，而不属于某个切片；一个 Query 可以读取多个切片。

  3. 原图级一对一匹配

     Hungarian Matching 在整幅图上执行，一个物体只匹配一次，并加入不同切片方案之间的集合一致性训练。

  因此论文真正的核心表述可以是：

  > We decouple the unit of computation from the unit of prediction: tiles are computational shards, while the original image remains the unit of
  > set prediction.

  ## 是否算端到端

  算，但要准确解释“端到端”：

  - 输入是一幅完整高分辨率图像；
  - 输出是一组完整原图坐标旋转框；
  - 只使用一个原图级集合损失；
  - 不使用 NMS、Soft-NMS、WBF 或人工切片框合并；
  - 切片和特征映射是模型内部的确定性计算；
  - 所有可学习部分由最终全图检测损失反向传播。

  为了控制显存，切片 Backbone 可以分批或流式执行。只要最终特征进入同一个全局 Decoder，并且训练时梯度能够回传，就仍然是逻辑和优化意义上的端到端。

  置信度阈值和固定最大输出数量仍然可以保留，因为这不是 NMS：它们不根据框之间的 IoU 删除预测。

  ## 第一版如何实现

  我建议用 RT-DETRv4 仓库里的 D-FINE-S/HGNetv2：

  1. 切片只运行 Backbone + Hybrid Encoder，不运行独立检测头。
  2. 将各切片的 P3/P4/P5 映射并融合成低分辨率全图特征图。
  3. 在全图特征上进行一次全局 Top-Q Query 初始化。
  4. 将 D-FINE Decoder 改成旋转框 Decoder。
  5. Hungarian Matcher 改为旋转框代价，并在原图标注上匹配。
  6. 推理直接输出全图 OBB，完全关闭 NMS。
  7. 随后加入跨切片旋转采样和不同网格的一致性训练。

  第一版可以先验证最核心的问题：

  > “特征级拼接 + 全图集合预测”能否在完全无 NMS 时超过“独立切片检测 + 旋转 NMS”。

  如果这个结论成立，再加入跨边界旋转注意力和 partition consistency，论文逻辑会非常完整。

  ## 需要提前解决的三个风险

  - Query 数量：全图目标可能远超标准 DETR 的 300 个 Query，需要根据 CODrone 每图目标数量分布设置，不能拍脑袋。
  - 训练显存：必须设计切片微批处理、特征缓存或梯度重计算。
  - 切片边缘特征：需要 overlap/halo 或跨切片采样，否则 Backbone 阶段已经丢失的上下文，Decoder 无法完全恢复。

  所以最终结论是：

  > 对，我们要做的就是一个“内部切片计算、全图集合预测、直接输出旋转框、无需 NMS 合并”的端到端高分辨率旋转检测器。

  它与 EdgeCrafter 的区别也很清楚：EdgeCrafter创新的是如何获得更强的紧凑表征；我们创新的是高分辨率分块计算下，如何仍然保持 DETR 的全图一对一集合
  预测性质。当前这还是研究方案与待验证假设，下一步需要专门做相似工作查新和最小原型验证。


# 切片的原因就是全分辨率处理显存占用很高，计算量超级大。我们的方案还保留切片的优点吗


• 保留，但需要把“切片的优势”拆开说清楚：

  > 我们的方案能保留低峰值显存、局部注意力和原始小目标分辨率；但不能让遍历全部 4K 像素的线性计算凭空消失。

  如果之前让你觉得“既完整处理所有像素，又能大幅降低全部 FLOPs”，那是不严谨的。

  ## 哪些优势能保留

   切片优势                             是否保留    原因
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   不把 4K Backbone 激活同时放入显存          是    切片按微批次送入共享 Encoder
  ───────────────────────────────────  ──────────  ──────────────────────────────────────────
   保留小目标原始像素                         是    不把整图缩放到 640/1024
  ───────────────────────────────────  ──────────  ──────────────────────────────────────────
   避免全图 Transformer 二次复杂度            是    AIFI 等密集注意力只在单个切片内执行
  ───────────────────────────────────  ──────────  ──────────────────────────────────────────
   降低总卷积 FLOPs                       不完全    所有区域仍要至少看一遍，重叠还会增加计算
  ───────────────────────────────────  ──────────  ──────────────────────────────────────────
   避免每个切片都运行一次 Decoder             是    我们只运行一次全图 Decoder
  ───────────────────────────────────  ──────────  ──────────────────────────────────────────
   避免 NMS/WBF                               是    最终只产生一个全图级集合

  ## 显存为什么仍然低

  正确架构应该是：

  切片1 ─┐
  切片2 ─┤ 分批运行 Backbone + 局部 Encoder
  切片3 ─┤
   ...   ─┘
            ↓
  只保留低分辨率 P3/P4/P5 特征
            ↓
  一次全图稀疏 Query Decoder

  峰值显存近似为：

  [
  M_{\mathrm{peak}}
  \approx
  M_{\mathrm{tile\ encoder}}(B_t)
  +
  M_{\mathrm{global\ features}}
  +
  M_{\mathrm{global\ decoder}}(Q)
  ]

  而不是完整 4K Backbone 所有层激活之和。

  以 3840×2160、256 通道、FP16 粗略估算：

  - stride-8 全图特征：约 63 MiB；
  - stride-16：约 16 MiB；
  - stride-32：约 4 MiB；
  - 三层原始特征缓冲合计约 83 MiB。

  实际训练还会增加梯度和中间状态，但仍远小于在完整 4K 图像上保存整套 Backbone 激活。切片 Encoder 可以配合微批次和梯度重计算。

  ## 注意力计算能明显下降

  全图 stride-32 特征大约是：

  [
  120\times68=8160\ \text{tokens}
  ]

  全局自注意力规模约为：

  [
  8160^2 \approx 6660万
  ]

  如果切成 12 个 1024×1024 块，每块 stride-32 是 32×32：

  [
  12\times(1024^2)\approx1260万
  ]

  仅这一部分理论规模约下降 5 倍。存在重叠时比例会稍低，但仍明显优于全图密集注意力。

  因此有一条必须遵守的设计约束：

  > 绝不能把切片特征拼起来以后，再对全图所有 P3/P4/P5 token 做一次密集 Transformer。

  那会把切片的优势重新吃掉。全图部分只能是固定数量 Query 的稀疏 deformable attention。

  ## 总计算量的真实情况

  我们的计算量是：

  # [
  C_{\mathrm{ours}}

  \sum_{t=1}^{T}C_{\mathrm{local\ encoder}}(I_t)
  +
  C_{\mathrm{global\ decoder}}(Q)
  ]

  普通切片检测是：

  # [
  C_{\mathrm{tile}}

  \sum_{t=1}^{T}
  \left[
  C_{\mathrm{encoder}}(I_t)+
  C_{\mathrm{decoder}}(Q_t)
  \right]
  +
  C_{\mathrm{NMS}}
  ]

  所以我们的主要计算节省来自：

  - 不在每个切片上重复运行完整 Decoder；
  - 不进行全图密集注意力；
  - 不执行每切片 300 Query 的最终预测；
  - 重叠区域在特征层融合，而不是产生大量重复框。

  例如普通方案 12 个切片、每块 300 Query，相当于处理 3600 个局部检测 Query；我们可能只需要一次 600～900 个全图 Query。具体数量要根据 CODrone 每图
  实例数分布确定。

  但是所有原始像素仍需要经过 Backbone。只要要求“全分辨率、全覆盖、不跳过任何区域”，特征提取就至少是：

  [
  \Omega(HW)
  ]

  这是无法规避的基本代价。普通切片加重叠甚至可能比无重叠整图 CNN 多 20%～60% FLOPs。

  ## 因此我们应该怎样定位“高效”

  不能写成：

  > 我们同时完整处理所有 4K 像素，却显著消除了所有计算量。

  更可信的论文表述是：

  > 我们提出一种内存受控的高分辨率端到端检测框架，通过局部分块编码和全图集合解码，在保留原始分辨率的同时，避免全图密集注意力、重复切片 Decoder 和
  > NMS 合并。

  也就是重点证明四件事：

  1. 峰值显存显著低于整图网络；
  2. 比传统“切片完整检测器 × N”减少 Decoder 计算；
  3. 端到端延迟不劣于切片 + NMS；
  4. 无 NMS 时，边界目标和重复预测反而更好。

  ## 我会修正上一版架构

  不采用“拼出全图特征后再做全局 Encoder”，而采用：

  > Tile-local Encoder + Lightweight Global Set Decoder

  具体是：

  - 所有切片完整覆盖，不做区域预算或区域丢弃；
  - Backbone/AIFI 只在切片内部运行；
  - 只保留必要的低分辨率特征；
  - 全局 Query 使用稀疏旋转采样读取相关切片；
  - Decoder 只运行一次；
  - 输出全图 OBB 集合，无 NMS。

  如果全图 P3 缓冲仍然太大，还可以进一步做“坐标索引的切片特征存储”，让 Query 只读取覆盖其参考框的切片，但这属于第二阶段优化。

  所以准确结论是：

  > 方案保留了切片最关键的显存与局部注意力优势，同时能够减少重复 Decoder 计算；但全覆盖高分辨率 Backbone 的线性计算仍然存在。

  这反而让研究命题更扎实：我们不是宣称违反计算规律，而是解决“切片计算与全图集合预测之间的结构矛盾”。
