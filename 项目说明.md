# 项目说明

本仓库以 RT-DETRv4/D-FINE 为底座，维护一个可训练、可评估、可诊断的旋转目标
检测框架。当前只保留两种 OBB 模型：原始角度回归和 O² ADR。

## 环境

统一使用：

```text
/icislab/volume1/liuxiaolong/anaconda3/envs/wyq-deim/bin/python
```

## 稳定能力

- DOTA quadrilateral/CODrone 数据读取、旋转增强和坐标转换；
- rotated IoU、rotated NMS、可视化和 DOTA-07/diagnostic AP；
- 原图求值，以及 DOTA-style tile 恢复原图后的合并求值；
- D-FINE direct-angle 与 O² ADR；
- 训练、推理、结构化诊断日志和 smoke tests。

公开 OBB 配置只有四个：

```text
configs/dfine/dfine_obb_angle.yml
configs/dfine/dfine_obb_o2.yml
configs/dfine/dfine_obb_angle_tile.yml
configs/dfine/dfine_obb_o2_tile.yml
```

其中 `_tile.yml` 只改变数据和求值协议，不引入第三种模型实现。

## 数据

当前主要验证数据为 DOTA 标注格式的 CODrone。新增数据集应复用通用 DOTA OBB
求值器，不复制带数据集名称的评估逻辑。原图、切片和 tiny split 说明位于
`docs/datasets/codrone/`。

## 目录与开发规则

- OBB 公共几何、数据、求值和诊断属于稳定框架层；
- O² 私有几何位于 `engine/rtv4/obb/methods/o2/`；
- O² 复现资料、工具和测试分别位于 `docs/research/o2/`、
  `tools/research/o2/`、`test/research/o2/`；
- 所有测试脚本统一放在 `test/`；
- 论文分析所需的角度、中心、rIoU、分布、显存和时间信息由代码直接写入日志，
  分析脚本只读取实验目录，不要求实验执行者手工整理数据。

新增数据集、模型、损失或模块必须遵循
`docs/framework/INTEGRATION_CONTRACT.md`，且不得破坏 direct-angle 与 O²。
