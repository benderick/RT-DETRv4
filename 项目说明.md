# 项目说明

本仓库以 RT-DETRv4/D-FINE 为底座，维护一个可训练、可评估、可诊断的旋转目标
检测框架。当前只保留两种 OBB 模型：原始角度回归和 O² ADR。

## 环境

统一使用：

```text
/icislab/volume1/liuxiaolong/anaconda3/envs/wyq-deim/bin/python
```

## 稳定能力

- DOTA quadrilateral/CODrone 数据读取、旋转增强和坐标转换；
- rotated IoU、rotated NMS、可视化和 DOTA-07/diagnostic AP；
- 原图求值，以及 DOTA-style tile 恢复原图后的合并求值；
- D-FINE direct-angle 与 O² ADR；
- 训练、推理、结构化诊断日志和 smoke tests。

公开 OBB 配置只有四个：

```text
configs/dfine/dfine_obb_angle.yml
configs/dfine/dfine_obb_o2.yml
configs/dfine/dfine_obb_angle_tile.yml
configs/dfine/dfine_obb_o2_tile.yml
```

其中 `_tile.yml` 只改变数据和求值协议，不引入第三种模型实现。

## 数据

当前主要验证数据为 DOTA 标注格式的 CODrone。新增数据集应复用通用 DOTA OBB
求值器，不复制带数据集名称的评估逻辑。原图、切片和 tiny split 说明位于
`docs/datasets/codrone/`。

## 目录与开发规则

- OBB 公共几何、数据、求值和诊断属于稳定框架层；
- O² 私有几何位于 `engine/rtv4/obb/methods/o2/`；
- O² 复现资料、工具和测试分别位于 `docs/research/o2/`、
  `tools/research/o2/`、`test/research/o2/`；
- 新研究方向必须使用 `docs/research/<idea>/`、`tools/research/<idea>/`、
  `test/research/<idea>/` 三个同名目录形成独立闭环；证伪阶段不得注册为稳定模型或
  修改 direct-angle/O² forward；
- 研究 idea 按 `docs/research/IDEA_LIFECYCLE.md` 经过 candidate、Stage 0、prototype
  和晋级/清退；失败实现退场，只在中央注册表和 `EXPERIENCE_LOG.md` 保留可复用经验；
- 多个 idea 必须按 `docs/framework/WORKTREE_CONTRACT.md` 使用独立分支和 worktree；
- 所有测试脚本统一放在 `test/`；
- 论文分析所需的角度、中心、rIoU、分布、显存和时间信息由代码直接写入日志，
  分析脚本只读取实验目录，不要求实验执行者手工整理数据。

新增数据集、模型、损失或模块必须遵循
`docs/framework/INTEGRATION_CONTRACT.md`，且不得破坏 direct-angle 与 O²。

## 项目日志

- 2026-08-30：引入 Git worktree 研究契约；`main` 只保留稳定框架，每个活动
  idea 使用独立分支、worktree 和分支内 manifest，中央注册表只保存退役经验。
- 2026-08-30：`pullback_distribution_refinement` 完整 Stage 0 结论为 `STOP`：局部
  ADR 能量与 rIoU 排序相关，但逐层 replay 使 mAP50-95 下降 `0.003960`、耗时
  增至 `4.159x`，证明确定性残差 logits 不是可直接最大化的联合后验。结论记入
  `EXP-2026-011`。全部已否决 idea 的实现、专用测试与声明为可丢弃的大缓存
  已清退，只保留注册表、经验卡、裁决报告和关键图。清理后全项目 `132` 项测试
  通过，`3` 项仅因当前环境无 CUDA 跳过；direct-angle/O² 稳定路径未受影响。
- 2026-08-29：`role_factored_metric_refinement` 完成唯一 72-epoch prototype。实际角色
  路由与数值恒等式全部验收通过；mAP50-95 提高 `+0.002264`，并将 shared-MHR 损坏的
  score-rIoU 对齐恢复到优于 O²（Pearson `+0.032387`、MAE `-0.001460`）。但严格阈值
  增益不足、paired-rIoU 的视频区间跨零，step 耗时增加 `+5.81%`；未通过预注册联合
  gate，登记为 `EXP-2026-010`。不把 role conflict 的成立偷换成当前 RFMR 方法成立。
- 2026-08-29：新建独立命题 `role_factored_metric_refinement`。冻结 MHR 结果表明，同一
  HBox→rIoU 干预使 mAP50-95 提高 `+0.004251`、paired-rIoU 的视频 bootstrap 下界为
  `+0.001443`，却使 score-rIoU Pearson/Spearman 下降 `0.088900/0.046931`、MAE 增加
  `0.006196`；稳定 criterion 的 VFL/FGL 又确实共享同一 HBox quality hook。Stage 0
  因而只裁定 shared-metric role conflict 成立。已建立唯一 FGL-only prototype：VFL
  逐层保持稳定 HBox quality，FGL 按 `[0,1/3,2/3,1]` 过渡到 rIoU；无新参数和推理改动，
  其余完整配置与稳定 O² 相同。全项目 `212` 项测试通过、`5` 项因当前环境无 CUDA 跳过。
- 2026-08-29：`metric_homotopy_refinement` 完成同 seed 72 epoch 裁决。mAP50-95 从
  `0.896561` 提高到 `0.900812`，AP85/90/95 与全部 11,461 个配对对象的高 rIoU
  几何均改善；但 score-rIoU Pearson 从 `0.647656` 降至 `0.558756`，训练 step 耗时增加
  `+7.61%`。联合命题未通过全部预注册 gate，登记为 `EXP-2026-009`，不在结果后偷换成
  FGL-only 或修改阈值继续抢救。
- 2026-08-29：冻结日志审计定位到 O²/DFINE 的质量监督仍使用忽略角度的 HBox IoU，
  与 matching、OBB regression 和最终 rIoU evaluator 不一致。11,461 个完整匹配对象上
  Stage 0 必要性 gate 通过；新增隔离的 `metric_homotopy_refinement` prototype，让 VFL
  与 FGL 的质量度量按 decoder 深度从 HBox IoU 零参数过渡到 rIoU。稳定 direct-angle
  与 O² 配置、criterion、推理路径均未修改，等待一次 UAV-ROD 72 epoch 裁决训练。
- 2026-08-29：`geometry_marginalized_attention` 与
  `metric_aligned_set_survival` 在完整、按视频隔离的正式审计中被否决，结论分别记录为
  `EXP-2026-007/008`；不靠延长训练或调 residual 抢救。
- 2026-08-28：`support_space_refinement` 的数学 seam 存在，但模型级效应未通过场景稳定
  gate，已按 `EXP-2026-006` 否决，未进入模型。
- 2026-08-27：`head_conditional_measure_queries` 完整 UAV-ROD Stage 0 通过，但隔离的
  adapter-only Stage 1 被预注册闸门否决：12 轮最佳 mAP50-95 为 `0.896320048`，未超过
  稳定 O² 的 `0.896323180`，而 adapter 已确认非零、有限并实际改变四层特征。结论登记
  为 `EXP-2026-005`；不延长训练、不补 loss，也不把该 prototype 并入稳定框架。
- 2026-08-26：登记 `geometric_measure_queries` Stage 0；冻结“同一 query 空间测度同时
  决定 cross-attention 取证与对称感知几何”的命题、完整集 OOF gate、停止条件和机制图，
  此阶段不改稳定模型、不训练。
- 2026-08-26：O² 六分量逐层精炼基线完成 UAV-ROD 从零训练与独立冻结 checkpoint 验收，全部必需 gate 通过。
- 2026-08-26：visible–silent Stage 0 在 UAV-ROD 完整集结论为 `NOT_SUPPORTED`；经验
  已进入 `EXP-2026-001`，不开发模型，并作为首个按生命周期清退的研究实现。
