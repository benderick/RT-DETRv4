__include__: [./dfine_obb_o2_fressdet.yml]
imports: [engine.data.dataset.moda_rgb_dataset]
output_dir: ./logs/moda/experiments/rgb/seed0_fp32

HGNetv2:
  in_channels: 3
diagnostics_source_evidence:
  pseudo_rgb_bands: [0, 1, 2]

# Dataset returns [B4, B2, B1]; source arrays and model code stay unchanged.
train_dataloader:
  dataset:
    type: MODARGBDetection
val_dataloader:
  dataset:
    type: MODARGBDetection
