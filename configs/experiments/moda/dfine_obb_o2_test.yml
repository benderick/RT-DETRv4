# Frozen checkpoint evaluation only. Never use official test to select epochs.
__include__: [./dfine_obb_o2.yml]
output_dir: ./logs/moda/dfine_obb_o2_test
val_dataloader:
  dataset:
    root: ./data/MODA/test
    expected_labels: 4885
    split_file: ~
