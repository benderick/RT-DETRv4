__include__: [
  '../../dfine/dfine_obb_angle.yml',
  '../../dataset/uav_rod_obb.yml',
]

output_dir: ./logs/uav_rod/dfine_obb_angle

# Use the same published O²-DFINE-M optimization budget as the O² run; only
# the declared method mechanisms differ.
train_dataloader:
  total_batch_size: 8

val_dataloader:
  total_batch_size: 8
